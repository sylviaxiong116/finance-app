services:
  - type: web
    name: finance-app-backend
    runtime: python
    buildCommand: cd backend && pip install -r requirements.txt && pip install uvicorn
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
